name: Archived EC2 deployment

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest

    steps:
      - name: Explain archived status
        run: |
          echo "The previous AWS EC2 deployment has been terminated."
          echo "This workflow is kept as a historical marker and does not deploy anything."
