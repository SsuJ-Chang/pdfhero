# Stage 1: HTTP only redirect (before SSL)
# Certbot will automatically add HTTPS config after running certbot --nginx

server {
    listen 80;
    server_name rj-tw.com;

    # 301 永久跳轉到 pdfhero 子域名
    return 301 https://pdfhero.rj-tw.com$request_uri;
}
